{
  "name": "notabene-notion-oauth",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth token-exchange proxy for the Nota Bene iOS app's Notion integration.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250528.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
